export type {
    Service,
    CartState,
    CartContextType,
} from './model/types';

export { CartActionTypes } from './model/types';
export { cartReducer, initialState } from './model/cartReducer';
