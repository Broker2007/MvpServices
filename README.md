# React + TypeScript + Vite

# Авиа-сервис: дополнительные услуги для пассажиров

Проект представляет собой MVP веб-приложения для выбора дополнительных услуг авиакомпании. Реализован с использованием современных технологий и архитектурных подходов.

## Основные возможности

- Просмотр списка дополнительных услуг (20 позиций)
- Выбор услуг с добавлением в корзину (toggle-логика)
- Полностью адаптивный интерфейс (mobile-first)
- Автоматический расчет общей стоимости
- Плавные анимации взаимодействия
- Чистая архитектура по методологии FSD

## Технологический стек

- **React 18** с TypeScript
- **Vite** для сборки
- **SCSS Modules** для стилей
- **Feature-Sliced Design (FSD)** для архитектуры
- **React Context + useReducer** для управления состоянием
- **ESLint** для контроля качества кода

## 🎯 Реализованные функции

### 1. Управление состоянием (Cart Context)

- Централизованное хранение состояния корзины
- Оптимизация производительности через `useMemo`
- Полная типизация на TypeScript
- Действия: добавление, удаление, очистка, загрузка услуг

### 2. Адаптивный дизайн (Mobile-First)

- **Мобильные устройства**:

  - 1 колонка карточек
  - Сворачиваемая корзина внизу экрана
  - Fixed-шапка и корзина
- **Планшеты (≥768px)**:

  - 2 колонки карточек
  - Улучшенные отступы
- **Десктоп (≥1024px)**:

  - 3 колонки карточек
  - Увеличенные отступы
  - Fixed-корзина справа

### 3. Интерактивные элементы

- **Карточки услуг**:

  - Toggle-кнопка "Добавить/Убрать"
  - Визуальная обратная связь через изменение цвета
  - Адаптивные изображения
- **Корзина заказа**:

  - Плавная анимация раскрытия/сворачивания
  - Автоматический пересчет суммы
  - Валидация кнопки "Оформить заказ"
  - Кастомный скроллбар

### 4. Система дизайна

- CSS-переменные для единообразия:

  - Цветовая палитра (`--color-primary`, `--color-danger`, etc.)
  - Типографика (`--font-s`, `--font-m`, `--font-l`)
  - Отступы (`--spacing-xs`, `--spacing-s`, `--spacing-m`, etc.)
  - Тени, радиусы скругления, анимации

## 🔧 Логика работы

### Добавление услуги

1. Пользователь кликает "Добавить" на карточке услуги
2. Проверяется, есть ли услуга уже в корзине
3. Если нет — услуга добавляется с пересчетом суммы
4. Кнопка меняется на "Убрать" (красный цвет)

### Удаление услуги

1. Из карточки: клик на "Убрать" (toggle-логика)
2. Из корзины: клик на "Удалить" в блоке заказа
3. Сумма автоматически пересчитывается

### Анимации

- Плавное раскрытие/сворачивание корзины
- Параллакс-эффект при наведении на кнопки
- Плавное появление карточек в корзине
- Оптимизация для избежания "дергания" скроллбара

## Особенности реализации

### Производительность

- Мемоизация функций контекста для избежания лишних ререндеров
- Оптимизированные CSS-анимации
- Ленивая загрузка не реализована (не требуется для MVP)

### Типизация

- Полная TypeScript типизация всех компонентов
- Строго типизированные action types для редьюсера
- Интерфейсы для всех сущностей (Service, CartItem, CartState)

## Установка и запуск

bash

# Установка зависимостей

npm install

# Запуск в режиме разработки

npm run dev

# Сборка для production

npm run build

# Линтинг кода

npm run lint

## Примечания

- Проект следует принципам **Clean Code** и **KISS**
- Все компоненты функциональные с использованием хуков
- Стили организованы по методологии **BEM** (через CSS Modules)
- Конфигурация Vite оптимизирована для быстрой разработки
